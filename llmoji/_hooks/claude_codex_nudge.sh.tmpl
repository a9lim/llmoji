#!/usr/bin/env bash
# Auto-generated by llmoji $LLMOJI_VERSION. DO NOT edit by hand —
# `llmoji install <provider>` regenerates this file from
# llmoji/_hooks/claude_codex_nudge.sh.tmpl. Edits will be lost.
#
# UserPromptSubmit hook: inject the kaomoji-reminder nudge as
# additional context right before the model generates each response.
# Replaces a stale CLAUDE.md / AGENTS.md instruction — fresh every
# turn, immune to instruction decay.
#
# Response shape (verified byte-identical between Claude Code and
# Codex; codex-rs/hooks/src/events/user_prompt_submit.rs):
#
#   { "hookSpecificOutput": {
#       "hookEventName": "UserPromptSubmit",
#       "additionalContext": "<msg>" } }
#
# `set -eu` plus `set -o pipefail` so a jq miswrite fails the hook
# instead of silently emitting garbage; the harness fail-opens on
# non-zero exit, so the worst case is "no nudge this turn."
set -euo pipefail

MSG=$NUDGE_MESSAGE_QUOTED

jq -nc --arg ctx "$MSG" '{
  hookSpecificOutput: {
    hookEventName: "UserPromptSubmit",
    additionalContext: $ctx
  }
}'
