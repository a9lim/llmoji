#!/usr/bin/env bash
# Auto-generated by llmoji $LLMOJI_VERSION. DO NOT edit by hand —
# `llmoji install hermes` regenerates this file from
# llmoji/_hooks/hermes_nudge.sh.tmpl. Edits will be lost.
#
# pre_llm_call hook: inject the kaomoji-reminder nudge as additional
# context right before each agent turn. Per the hermes-agent v0.11.0
# Event Hooks docs, pre_llm_call is "the only hook whose return
# value is used"; the response shape is a bare ``{context: "<msg>"}``
# object (no envelope), or ``null`` to skip.
#
# `set -eu` plus `set -o pipefail` so a jq miswrite fails the hook
# instead of silently emitting garbage; hermes fail-opens on non-zero
# exit, so the worst case is "no nudge this turn."
set -euo pipefail

MSG=$NUDGE_MESSAGE_QUOTED

jq -nc --arg ctx "$MSG" '{ context: $ctx }'
